# Развёртывание Qwen3-8B в LM Studio

## 📋 Требования

- **RAM:** 32 GB ✅
- **VRAM:** 16 GB ✅ (модель использует ~8-10 GB)
- **ОС:** Windows 10/11
- **LM Studio:** версия 0.3.x или новее

---

## 🔧 Шаг 1: Установка LM Studio

1. Скачайте LM Studio с официального сайта:
   👉 https://lmstudio.ai/

2. Установите приложение (стандартная установка)

3. Запустите LM Studio

---

## 📥 Шаг 2: Загрузка модели Qwen3-8B

### Вариант A: Через интерфейс LM Studio

1. Откройте LM Studio
2. Перейдите на вкладку **"Discover"** (иконка лупы слева)
3. В поиске введите: `qwen3-8b`
4. Найдите модель: **Qwen/Qwen3-8B-GGUF** или **lmstudio-community/Qwen3-8B-GGUF**
5. Выберите квантизацию:
   - **Q4_K_M** (~5 GB) — баланс качества и скорости
   - **Q6_K** (~7 GB) — хорошее качество
   - **Q8_0** (~9 GB) — лучшее качество для 16 GB VRAM ✅ рекомендую
6. Нажмите **Download**

### Вариант B: Скачать вручную с Hugging Face

1. Перейдите: https://huggingface.co/Qwen/Qwen3-8B-GGUF
2. Скачайте файл `.gguf` нужной квантизации
3. Поместите в папку: `C:\Users\<ИМЯ>\.lmstudio\models\`

---

## ⚙️ Шаг 3: Настройка модели

### Загрузка модели в память

1. Перейдите на вкладку **"Chat"** или **"Developer"**
2. В верхней панели нажмите **"Select a model to load"**
3. Выберите загруженную модель Qwen3-8B
4. Дождитесь загрузки (1-2 минуты)

### Рекомендуемые параметры

```yaml
# Настройки модели (правая панель)
Context Length: 8192        # Можно увеличить до 32768
Temperature: 0.7            # Для обучения лучше 0.3-0.5
Top P: 0.9
GPU Layers: максимум        # Для 16 GB VRAM — все слои на GPU
```

---

## 🌐 Шаг 4: Запуск локального API сервера

### Включение сервера

1. Перейдите на вкладку **"Developer"** (иконка `</>` слева)
2. Слева вверху включите тумблер **"Status: ..."** → станет **"Running"**
3. Убедитесь, что видите:
   ```
   Server running on: http://localhost:1234
   ```

### Настройки сервера

- **Port:** 1234 (по умолчанию)
- **CORS:** Enabled (для веб-приложений)
- **Verbose logging:** можно включить для отладки

---

## 🧪 Шаг 5: Проверка API

### Через PowerShell

```powershell
# Проверка доступности сервера
Invoke-RestMethod -Uri "http://localhost:1234/v1/models" -Method GET

# Тестовый запрос
$body = @{
    model = "qwen3-8b"
    messages = @(
        @{ role = "user"; content = "Привет! Объясни что такое Python?" }
    )
    temperature = 0.7
} | ConvertTo-Json -Depth 3

Invoke-RestMethod -Uri "http://localhost:1234/v1/chat/completions" `
    -Method POST `
    -ContentType "application/json" `
    -Body $body
```

### Через Python

```python
import requests

response = requests.post(
    "http://localhost:1234/v1/chat/completions",
    json={
        "model": "qwen3-8b",
        "messages": [
            {"role": "user", "content": "Привет! Что такое Python?"}
        ],
        "temperature": 0.7
    }
)

print(response.json()["choices"][0]["message"]["content"])
```

---

## 🔗 Шаг 6: Интеграция с проектом

### Структура API (OpenAI-совместимый)

LM Studio предоставляет API, совместимый с OpenAI:

| Endpoint | Описание |
|----------|----------|
| `GET /v1/models` | Список загруженных моделей |
| `POST /v1/chat/completions` | Чат с моделью |
| `POST /v1/completions` | Генерация текста |
| `POST /v1/embeddings` | Векторные представления |

### Переменные окружения для проекта

Создайте файл `.env` в корне проекта:

```env
# LM Studio API
LM_STUDIO_BASE_URL=http://localhost:1234/v1
LM_STUDIO_MODEL=qwen3-vl-8b

# Настройки генерации
LLM_TEMPERATURE=0.5
LLM_MAX_TOKENS=2048
```

### Пример интеграции с LangChain

```python
from langchain_openai import ChatOpenAI

# Подключение к LM Studio
llm = ChatOpenAI(
    base_url="http://localhost:1234/v1",
    api_key="not-needed",  # LM Studio не требует ключ
    model="qwen3-8b",
    temperature=0.5,
)

# Использование
response = llm.invoke("Объясни что такое декоратор в Python")
print(response.content)
```

---

## ⚠️ Решение проблем

### Модель не загружается

- Проверьте, достаточно ли VRAM (нужно ~8-10 GB для Q8_0)
- Попробуйте меньшую квантизацию (Q4_K_M вместо Q8_0)
- Уменьшите Context Length до 4096

### Медленная генерация

- Убедитесь, что все слои на GPU (GPU Layers = max)
- Закройте другие приложения, использующие GPU
- Уменьшите Max Tokens

### Ошибка подключения к API

- Проверьте, что сервер запущен (вкладка Developer)
- Проверьте порт: по умолчанию 1234
- Отключите firewall/антивирус для localhost

---

## ✅ Чеклист готовности

- [ ] LM Studio установлен
- [ ] Модель Qwen3-8B скачана
- [ ] Модель загружена в память
- [ ] Локальный сервер запущен (порт 1234)
- [ ] API отвечает на тестовые запросы
- [ ] Создан файл `.env` с настройками

---

## 📚 Полезные ссылки

- [LM Studio Documentation](https://lmstudio.ai/docs)
- [Qwen3-8B на Hugging Face](https://huggingface.co/Qwen/Qwen3-8B-GGUF)
- [LangChain + Local LLMs](https://python.langchain.com/docs/integrations/llms/)

---

**Следующий шаг:** После настройки LM Studio переходим к созданию агента-репетитора! 🚀

